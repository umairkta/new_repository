git config --global user.name "username"

git config --global user.email "useremail"

git init => for initializing git in pwd

git status =>list all untracked files

git add -A => adding all changes to starting area

git commit -m "commit message" => adding staged files to local repository

git remote add origin  https://github.com/umairkta/git-intro.git
                 ^
        easy to remember name

git remote -v        

https://github.com/umairkta/new_repository.git


git romote add remote_name remote_path

git push remote_name branch_name

git branch => list all branches

git branch branch_name => creates a new branch

this new line in student

from master:
git merge branch_name